import { makeStyles } from "@material-ui/core/styles";

export const useStyle = makeStyles({
  root: {
    padding: "10px",
    position: "relative",
    top: "50px"
  },
  item: {
    padding: "10px"
  }
});
